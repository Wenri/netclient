name: Release (disabled - requires gravitl/devops)

on:
  workflow_dispatch:
    inputs:
      version:
        description: "new version number"
        required: true

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow requires gravitl/devops and is disabled"
    # Original:
    # uses: gravitl/devops/.github/workflows/netclientRelease.yml@master
    # with:
    #   version: ${{ inputs.version }}
    # secrets: inherit
